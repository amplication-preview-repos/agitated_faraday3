/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { RepairService } from "../repair.service";
import { RepairCreateInput } from "./RepairCreateInput";
import { Repair } from "./Repair";
import { RepairFindManyArgs } from "./RepairFindManyArgs";
import { RepairWhereUniqueInput } from "./RepairWhereUniqueInput";
import { RepairUpdateInput } from "./RepairUpdateInput";
import { PartsUsedFindManyArgs } from "../../partsUsed/base/PartsUsedFindManyArgs";
import { PartsUsed } from "../../partsUsed/base/PartsUsed";
import { PartsUsedWhereUniqueInput } from "../../partsUsed/base/PartsUsedWhereUniqueInput";

export class RepairControllerBase {
  constructor(protected readonly service: RepairService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Repair })
  async createRepair(@common.Body() data: RepairCreateInput): Promise<Repair> {
    return await this.service.createRepair({
      data: {
        ...data,

        motorcycle: data.motorcycle
          ? {
              connect: data.motorcycle,
            }
          : undefined,

        mechanic: data.mechanic
          ? {
              connect: data.mechanic,
            }
          : undefined,
      },
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        date: true,
        cost: true,
        description: true,

        motorcycle: {
          select: {
            id: true,
          },
        },

        mechanic: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Repair] })
  @ApiNestedQuery(RepairFindManyArgs)
  async repairs(@common.Req() request: Request): Promise<Repair[]> {
    const args = plainToClass(RepairFindManyArgs, request.query);
    return this.service.repairs({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        date: true,
        cost: true,
        description: true,

        motorcycle: {
          select: {
            id: true,
          },
        },

        mechanic: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Repair })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async repair(
    @common.Param() params: RepairWhereUniqueInput
  ): Promise<Repair | null> {
    const result = await this.service.repair({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        date: true,
        cost: true,
        description: true,

        motorcycle: {
          select: {
            id: true,
          },
        },

        mechanic: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Repair })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateRepair(
    @common.Param() params: RepairWhereUniqueInput,
    @common.Body() data: RepairUpdateInput
  ): Promise<Repair | null> {
    try {
      return await this.service.updateRepair({
        where: params,
        data: {
          ...data,

          motorcycle: data.motorcycle
            ? {
                connect: data.motorcycle,
              }
            : undefined,

          mechanic: data.mechanic
            ? {
                connect: data.mechanic,
              }
            : undefined,
        },
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          date: true,
          cost: true,
          description: true,

          motorcycle: {
            select: {
              id: true,
            },
          },

          mechanic: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Repair })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteRepair(
    @common.Param() params: RepairWhereUniqueInput
  ): Promise<Repair | null> {
    try {
      return await this.service.deleteRepair({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          date: true,
          cost: true,
          description: true,

          motorcycle: {
            select: {
              id: true,
            },
          },

          mechanic: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/partsUseds")
  @ApiNestedQuery(PartsUsedFindManyArgs)
  async findPartsUseds(
    @common.Req() request: Request,
    @common.Param() params: RepairWhereUniqueInput
  ): Promise<PartsUsed[]> {
    const query = plainToClass(PartsUsedFindManyArgs, request.query);
    const results = await this.service.findPartsUseds(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        partName: true,
        partNumber: true,
        quantity: true,
        cost: true,

        repair: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/partsUseds")
  async connectPartsUseds(
    @common.Param() params: RepairWhereUniqueInput,
    @common.Body() body: PartsUsedWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      partsUseds: {
        connect: body,
      },
    };
    await this.service.updateRepair({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/partsUseds")
  async updatePartsUseds(
    @common.Param() params: RepairWhereUniqueInput,
    @common.Body() body: PartsUsedWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      partsUseds: {
        set: body,
      },
    };
    await this.service.updateRepair({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/partsUseds")
  async disconnectPartsUseds(
    @common.Param() params: RepairWhereUniqueInput,
    @common.Body() body: PartsUsedWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      partsUseds: {
        disconnect: body,
      },
    };
    await this.service.updateRepair({
      where: params,
      data,
      select: { id: true },
    });
  }
}
